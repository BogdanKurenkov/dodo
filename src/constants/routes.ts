interface Routes {
  [key: string]: string;
}

export const routes: Routes = {
  about: "routes.about",
  participate: "routes.participate",
  results: "routes.results",
  faq: "routes.faq",
  contacts: "routes.contacts",
};
